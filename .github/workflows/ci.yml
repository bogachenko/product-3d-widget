name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'CI is temporarily paused while the implementation branch is updated atomically.'
